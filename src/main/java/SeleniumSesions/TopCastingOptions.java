package SeleniumSesions;

import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chromium.ChromiumDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOptions {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

//		WebDriver driver = new ChromeDriver();//Valid top casting - Cross Browser - Yes
//		WebDriver driver = new FirefoxDriver();	//Valid top casting - Cross Browser - Yes
//		WebDriver driver = new RemoteWebDriver(remote url,capabilities); // Valid - Remote Executions
//		SearchContext driver = new FirefoxDriver();	//Valid but less number of methods - So not useful
//		SearchContext driver = new RemoteWebDriver(remote url,capabilities);	//Valid but less number of methods - So not useful
//		RemoteWebDriver driver = new ChromeDriver(); //Valid top casting - Cross Browser - Yes
//		driver.
		//Always refer Selenium WebDriver github branch
		//thoughtworks initially worked on Selenium
		//org/openqa/selenium
		
//		WebDriver driver = new ChromiumDriver();// We cannot create objects since CHrimium has only protected constructor
	}
}
